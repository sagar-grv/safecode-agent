export const supportedTaskCases = [
  "Summarize this CSV dataset.",
  "Count rows and list columns.",
  "Profile the numeric columns.",
  "Calculate basic statistics for each numeric column.",
  "Analyze revenue by region.",
  "Create a revenue bar chart by region.",
  "Group monthly revenue by region.",
  "Find the highest-revenue region.",
  "Generate a JSON dataset summary.",
  "Report the number of records.",
  "Inspect the CSV schema.",
  "Calculate minimum and maximum numeric values.",
  "Produce a machine-readable profile.",
  "Summarize a customer export.",
  "Inspect a product inventory CSV.",
  "Profile a support-ticket export.",
  "Summarize monthly sales records.",
  "Analyze an operations report.",
  "Describe numeric distributions.",
  "Count populated numeric values.",
  "Build a safe dataset overview.",
  "Generate an SVG chart of regional revenue.",
  "Save summary statistics as JSON.",
  "Review columns and row counts.",
] as const;

export const unsafeCodeCases = [
  'import os\nos.system("id")',
  'import subprocess\nsubprocess.run(["id"])',
  'import socket\nsocket.create_connection(("example.com", 80))',
  'import requests\nrequests.get("https://example.com")',
  'from urllib.request import urlopen\nurlopen("https://example.com")',
  'exec("print(1)")',
  'eval("1 + 1")',
  'compile("print(1)", "x", "exec")',
  '__import__("os")',
  'open("/etc/passwd", "r")',
  'open("../outside.txt", "w")',
  'path = "input.csv"\nopen(path, "r")',
  "while True:\n    pass",
  "import threading\nthreading.Thread(target=lambda: None).start()",
  "import multiprocessing\nmultiprocessing.Process().start()",
  "import pickle\npickle.loads(b'')",
  "import ctypes\nctypes.CDLL(None)",
  "import pathlib\npathlib.Path('/').iterdir()",
  "import shutil\nshutil.rmtree('/')",
  "globals()",
  "getattr(object(), '__class__')",
  "input('secret: ')",
  "breakpoint()",
  "class X:\n    def __getattribute__(self, name):\n        return name",
] as const;
